<Window x:Class="DrumPracticeStudio.Views.YouTubePlaylistImportModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Importar playlist de YouTube"
        Icon="/Assets/DrumPracticeStudio.ico"
        Width="520" Height="300"
        ResizeMode="NoResize"
        Background="{StaticResource AppBackground}"
        Foreground="{StaticResource TextPrimary}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="¿Dónde quieres importar esta playlist?"
                   FontSize="21" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1"
                   Text="Puedes crear una playlist nueva con el nombre de YouTube o añadir todos sus vídeos a una o varias playlists que ya existan."
                   Foreground="{StaticResource TextSecondary}"
                   TextWrapping="Wrap" Margin="0,7,0,18" />

        <StackPanel Grid.Row="2">
            <Button Style="{StaticResource PrimaryButton}"
                    Content="Crear una playlist nueva"
                    Click="OnCreateNewClick"
                    HorizontalAlignment="Stretch"
                    AutomationProperties.AutomationId="CreateNewPlaylistFromYouTubeButton" />
            <Button x:Name="ExistingPlaylistButton"
                    Style="{StaticResource SecondaryButton}"
                    Content="Añadir a playlist existente"
                    Margin="0,10,0,0"
                    Click="OnUseExistingClick"
                    HorizontalAlignment="Stretch"
                    AutomationProperties.AutomationId="UseExistingPlaylistForYouTubeButton" />
        </StackPanel>

        <Button Grid.Row="3"
                Style="{StaticResource SecondaryButton}"
                Content="Cancelar"
                HorizontalAlignment="Right"
                Margin="0,18,0,0"
                Click="OnCancelClick" />
    </Grid>
</Window>
