<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Este diccionario no redefine la plantilla visual: reutiliza la ScrollBar global de App.xaml
         y la vuelve a declarar dentro de los controles compuestos que crean barras en su plantilla. -->
    <Style x:Key="DrumlessScrollViewerScope" TargetType="{x:Type ScrollViewer}">
        <Style.Resources>
            <Style TargetType="{x:Type ScrollBar}"
                   BasedOn="{StaticResource {x:Type ScrollBar}}" />
        </Style.Resources>
    </Style>

    <Style x:Key="DrumlessListBoxScope" TargetType="{x:Type ListBox}">
        <Style.Resources>
            <Style TargetType="{x:Type ScrollBar}"
                   BasedOn="{StaticResource {x:Type ScrollBar}}" />
        </Style.Resources>
    </Style>

    <Style x:Key="DrumlessComboBoxScope" TargetType="{x:Type ComboBox}">
        <Style.Resources>
            <Style TargetType="{x:Type ScrollBar}"
                   BasedOn="{StaticResource {x:Type ScrollBar}}" />
        </Style.Resources>
    </Style>
</ResourceDictionary>
